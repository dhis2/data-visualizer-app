import React from 'react';

const ItemIcon = ({ backgroundColor }) => (
    <div
        style={{
            backgroundColor,
            minHeight: 6,
            minWidth: 6,
            margin: '0px 5px',
        }}
    />
);

export default ItemIcon;
