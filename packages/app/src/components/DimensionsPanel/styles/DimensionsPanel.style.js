import { colors } from '@dhis2/ui'

export const styles = {
    divContainer: {
        height: '100%',
        display: 'flex',
        flexDirection: 'column',
        backgroundColor: colors.grey050,
    },
}
