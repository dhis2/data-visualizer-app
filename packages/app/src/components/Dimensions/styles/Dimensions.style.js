import { colors } from '../../../colors';

export const styles = {
    divContainer: {
        backgroundColor: colors.snow,
        width: 250,
        padding: '12px 0 12px 12px',
    },
    searchIcon: {
        color: colors.grey,
    },
    textField: {
        marginBottom: 12,
    },
};
