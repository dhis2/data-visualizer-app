import { colors } from '../../../modules/colors';

export const styles = {
    divContainer: {
        backgroundColor: colors.snow,
        padding: '8px 0 8px 8px',
    },
    textField: {
        marginBottom: '12px',
        width: 'calc(100% - 8px)',
    },
};
