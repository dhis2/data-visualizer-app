import { colors } from '../../../modules/colors';

export const styles = {
    divContainer: {
        backgroundColor: colors.snow,
        padding: '12px 0px 0 12px',
    },
    searchIcon: {
        color: colors.grey,
    },
};
