import { colors } from '../../../modules/colors';

export default {
    flat: {
        backgroundColor: colors.accentPrimary,
        color: colors.white,
        fontSize: '13px',
        padding: '0 15px',
        boxShadow: 'none',
    },
};
