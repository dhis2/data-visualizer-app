import { colors } from '@dhis2/analytics';

export default {
    flat: {
        backgroundColor: colors.accentPrimary,
        color: colors.white,
        fontSize: '13px',
        padding: '6px 15px',
        boxShadow: 'none',
    },
};
