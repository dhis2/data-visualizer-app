import React from 'react';

const ItemIcon = ({ backgroundColor }) => (
    <div
        style={{
            backgroundColor,
            height: 6,
            width: 6,
            margin: '0px 5px',
        }}
    />
);

export default ItemIcon;
