import React from 'react';
import { colors } from '../colors';

const style = {
    minWidth: '300px',
    backgroundColor: colors.lightGrey,
};

const Dimensions = props => {
    return <div style={style}>Dimensions</div>;
};

export default Dimensions;
