import React from 'react';
import { colors } from '../colors';

const style = {
    minWidth: '300px',
    borderRight: `1px solid ${colors.charcoalGrey}`,
};

const ChartTypeSelector = props => {
    return <div style={style}>ChartTypeSelector</div>;
};

export default ChartTypeSelector;
